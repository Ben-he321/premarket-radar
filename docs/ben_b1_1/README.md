# Ben B1.1 数据桥接与完整事件回放

本轮接续原 B1 提交 e769f69391e6a4995901129f625f8ebb31920db7，使用独立分支 `codex/ben-b1-1-replay`。原 66 候选、60 KEEP / 5 排除 / 1 UNKNOWN 分类和交易参数保持冻结。

## 实际入口

- `src.ben_b1.replay.ReplayEngine`：按可用事件时钟推进信号、订单、部分成交、两腿止损、下一常规盘生效的移动止损、主动退出、财报版本、公司行动、现金/融资及恢复。
- `src.ben_b1.b11_research`：加载真实固定样本输入；预热到候选日期，之后逐日推进至自然结算或固定终点。20 个样本是各自独立的 5500 美元 P50 工程账户，不能拼成共同资金组合。
- `src.ben_b1.credentials`：仅桥接本项目及明确关联项目的真实配置入口；凭证、身份失败、权限、历史范围与成功空响应分别报告。
- `src.ben_b1.b11_data`：固定样本选择、独立分页缓存、原始行情及实际下载收据。
- `src.ben_b1.b11_coverage`、`src.ben_b1.b11_deliver`：只读汇总和白名单打包；不会下载数据或重新计算策略。
- `ben_b1_1_status.py`：只读结果页，没有交易、下载或研究启动按钮。

## 本机运行与配置

沿用已验证的 `premarket-radar-ai-m1/.venv/Scripts/python.exe` 和现有 requirements。实际数据放在用户目录下 `BenAITradingData/ben-b1-1-replay-20260914`，独立于 Git 工作目录，机器重启后保留。ZIP 不包含原始行情库或完整检查点。

Alpaca 沿用关联项目 `.streamlit/secrets.toml` 的 `ALPACA_API_KEY` / `ALPACA_SECRET_KEY`，无需重复复制。Finnhub 的实际原 Provider 字段为顶层 `FINNHUB_API_KEY`；未配置时仍可运行工程测试和已有发行人资料诊断。该字段存在并不证明历史日历权限，更不自动证明历史计划在当时可知。

本轮采用 SIP，不回退 IEX 或合成研究输入。新取得数据的下载时间与历史事件时钟分开；未记录的历史网络接收时间保持 UNKNOWN。最终历史日线在收盘后一分钟可用是工程研究假设，不能当作 Basic 在当时实时收到数据的证据。

## 冻结范围与证据层

候选选择区间固定为 2026-01-02 至 2026-09-11，沿原日线粗筛按日期和既定身份哈希排序取前 20。保留排除项，不用后续收益或成交资格换样本。供应商标准价格字段与独立逐点核验分层。财报 A 为事前计划证据，B 为有界连续实际发布日期的事后诊断，C 未知禁止入场。B 不得重命名为 PIT。

价格形态、2R、7%、16:05 决策、16:15 到期、5 秒报价、0.30% 价差和 1% 买价上限均保持。只有排序需要且主排序并列时，未知量能才阻断必要的次级排序；不把缺失量能变成新增入场硬门槛，也不填零。

## 恢复和验证

每样本的 `RUN_SPEC.json` 固定源码及输入哈希；`checkpoint.json` 原子保存事件、报价库存和账务。源码或既有输入改变时必须新建版本，旧输出不覆盖。Windows 短暂文件占用允许有界重试，持续失败明确报错，旧检查点保持可恢复。

完整本地状态留在数据目录。`RECOVERY_IDEMPOTENCY.json` 是对真实回放最后一批事件重复执行后状态哈希相同的证据。验收包提供证明及源码；单独持有 ZIP 不能恢复被排除的原始行情与完整检查点。

工程测试使用 pytest 的独立临时数据，不写入研究数据目录。运行测试：`python -m pytest tests/test_ben_b1*.py`。工程通过、真实历史模型成交、全池收益验证分别报告，不能互相替代。

本轮不启动 Ben 前向账户，不修改原 M20/U 服务或账本，不购买服务，不调用付费模型，不自动合并 main。
