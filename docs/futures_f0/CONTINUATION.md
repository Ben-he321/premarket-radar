# F0 2026-09-15 续作：赠额下载与真实样本核对

本次延续 `codex/futures-f0-pilot`，不改变冻结策略、原始四小时轮次或股票服务。
新轮次使用原数据根下的 `continuations/20260915T204308Z`；原报告、协议、基准和验收包保留。
历史数据标价上限为本轮累计 10 USD，现金采购、订阅及付费模型调用为 0。

## 已接通

`acquisition.Continuation` 从本 worktree 的配置加载 Databento Key 和原数据路径，
核验新轮次最长四小时与旧产物哈希，再执行明确合约的免费映射、真实估价和受限下载。
Key 不出现在命令、日志、源码或验证包中；不加载 `.env`。

`BudgetGate` 依据真实账户网页档案、API Key 与账户的绑定、赠额适用范围及官方先抵扣条款，
允许正标价历史请求。真实价格原样进入追加式预算账本；失败或不确定请求不释放预留。
每次收费请求前重新估价、检查原截止时间、月限、赠额及证据新鲜度；不自动充值或重试。
账户证据过期后停止新收费请求。刷新须另存真实新证据并接入账本链，当前没有自动刷新功能。

12 个真实请求已完成，合计标价预留 0.004445128142 USD；下载后用量页面出现 GLBX.MDP3。
当前账单应付显示 0.00 USD，精确赠额核销仍未独立对账。估价不是银行付款或最终扣费凭据。
12 份真实缓存另做了禁用网络的复用验证，原 receipt、来源哈希和正价账本未改变。

## 数据和资格分层

取得 ESZ4/MESZ4 的 2024-09-30 至 10-01 边界样本，
以及六市场 13 个具体合约的 2025-03-03 至 03-04 样本。
12 份 CSV 实际共 8,506 行、15 个具体合约，解析隔离为 0；这些不是四年完整历史。

`vendor` 保留原始纳秒、未知哨兵值、证券定义版本、结算标志与修订消息。
`contract_snapshot` 核对 13 个候选的数量、tick、币种、交易所和单位，
将 instrument activation 与已核实首个交易日期分开。
玉米每一美分报价的乘数是 ZC 50 USD、MZC 5 USD，不能直接用蒲式耳数量作为美元乘数。

`reconstruction` 已由真实 ES/MES 小样本重建 23 小时交易日，并用分钟数据独立核对最后一小时；
两份 OHLCV 五字段均相符。它始终输出 `research_qualified=false`，不会制造研究清单或账户。

日历修正依据 [CME 2021-06-21 通知](https://www.cmegroup.com/notices/electronic-trading/2021/06/20210621.html)：
ES/MES 自 2021-06-28 交易日起取消 15:15–15:30 Chicago 暂停；此前模板不能用于整个研究期。
具体假日仍需逐日核验。旧数据根的原登记表和报告没有覆盖。

真实 statistics 显示该日 final settlement 于下一时段开盘后才发布；
`ts_ref` 的 UTC 午夜是日期标签，不能当作结算定价或发布时间。
历史捕获时间、可推导的 CME 发送时间、实际下载接收时间分别保留。

## 未完成与恢复

F0/F1 的六个成本组合和两个假设保证金层均未运行，收益、回撤及策略门槛为 NA。
当前缺少历史 OHLCV 对后续 break/correction 的保真说明、完整期内的日历/交割期限、
以及 raw→可用于账户的逐日数据整合核验。不能用桶结束时间或手填 `verified` 冒充证据。
自建因果日线原则可行，仍须证明输入就是当时可见的信息前缀；并非要求任意等待一个发布时刻。

恢复入口为该轮 `RUNBOOK.md`、`ROUND_STATE.json`、`ROUND_PROGRESS.json` 和原根下 `credit_budget/journal.jsonl`。
原 `FROZEN_PROTOCOL.json` 的时限不改。不能通过再次运行 `preflight` 覆盖旧能力报告。
以后有合格清单时使用 `run-normalized --round-dir <已授权轮次> --input-manifest <已核验清单>`，
先验证期限与原文件；无法验证的新轮次连错误记录也不会写到旧目录。

本轮 229 项 F0 工程测试通过，含费用门禁、缓存、资源中断、单位转换、
换月信号与执行证据绑定、旧目录保护。测试不构成真实收益或可交易性验收。
