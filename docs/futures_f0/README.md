# Futures F0：独立历史工程试验

2026-09-15 晚间续作已接通赠额下载并核对真实小样本，详见
[续作状态与恢复](CONTINUATION.md)。下面保留首轮工程说明；其中零报价限制和缺 Key 状态由续作说明更新，
原轮次报告及真实矩阵未运行的历史记录不覆盖。

目标仓库为 `Ben-he321/premarket-radar`，本轮分支 `codex/futures-f0-pilot`，起点 `cfec4746d54c45f7bf31beb7c246709b99c9f6d4`。全部新增实现位于 `src/futures_f0/`，没有修改股票执行入口、页面或账本。原 M20/U 服务继续使用它自己的工作目录。

本轮唯一原始要求为用户提供的《Ben_F0_11500美元_多品种期货趋势与浮盈加仓_小测任务_20260915.txt》。冻结协议保存在本轮持久数据目录 `FROZEN_PROTOCOL.json`，包含附件哈希、实际冻结时间和本轮四小时截止时间。不得通过改协议时间自动延长授权。

## 本机目录与运行器

- 新源码：`C:/Users/benhe/OneDrive/Documentos/GITHUB/premarket-radar-futures-f0`
- 新数据与证据：`D:/BenAITradingData/futures-f0-pilot-20260915T104739Z`
- 已验证 Python 3.13：`C:/Users/benhe/OneDrive/Documentos/GITHUB/premarket-radar-ai-m1/.venv/Scripts/python.exe`

仅复用原虚拟环境中的 Python/通用依赖；没有安装或升级依赖，没有导入股票结算内核。所有计算从新工作目录运行。D 盘是本机持久目录，正常重启后文件保留；它不是云端存储或异地备份。行情、数据库和私密配置不进 Git。

## 配置及最少数据需求

在新工作目录的 `.streamlit/secrets.toml` 填写 `DATABENTO_API_KEY`；也可使用同名环境变量。`FUTURES_F0_DATA_DIR` 指向上述独立 D 盘目录。示例配置只有空值。模块不会自动加载 `.env`，不会扫描全盘找 Key。

凭证之外，还需核对本账户真实历史额度的余额、到期日和使用授权，不能把公开注册优惠当成已经到账。本轮现金采购额度为 0，未购买服务或调用付费模型。没有 Key 时继续离线工程，真实期货数据和费用报价保持未验证。

`python -B -m src.futures_f0 preflight` 只记录配置和数据资格；它不是下载成功证明。`MetadataClient.estimate` 使用免费元数据接口检查所选明确合约的行数、字节和费用，最多重试三次。不会请求整个 `.FUT` 父级，因为它包含价差。每批最多 31 天；生产日线由经过核验的交易时段内小时或必要分钟数据聚合，UTC 自然日日线被拒绝。

当前下载器只放行它自身再次取得的零金额真实报价，流式保存并核验缓存哈希；正金额报价一律阻断，待取得真实账户额度/授权证据后单独接通，不存在“已授权信用余额”布尔开关。中断保留 `.partial`，不自动重复计费。只获得原始 CSV 不等于研究数据合格。

## 已实现的工程与尚未接通的接口

- 有界滚动 Wilder ATR20、此前55/20时段通道；长短对称，下一完整时段执行；止损、跳空、跟踪保护、退出优先及不倒填错过意图。
- 11500美元共用净权益、整数手数、各层/账户保证金、名义敞口、金属组及回吐风险限制；F0不加仓，F1最多两次，拒绝的档位同样消耗资格。
- 期货逐日盯市，保证金不当成本/额外资产，不套用股票全价买入或 T+1。原始成交票据另用 FIFO 独立复算，包含部分减仓、换月和期末未平仓。
- 明确的同规格合约换月助手使用已知两日成交量和已验证期限；成交仍分旧合约退出、新合约进入两腿。不同乘数产品迁移尚不支持，会阻断，不能自动把 MGC 仓位缩放成 1OZ。
- 日线开盘/止损是成交代理；发布结算价时转研究现金也是代理，未经真实券商/清算行到账验证。缺可成交证据时保留未执行状态，不填理想价。缺负现金融资费率时标记绩效资格阻断，不注入资金。
- 有明确真实数据清单的 `run-normalized` 可按六个冻结组合顺序执行，校验输入/检查点哈希并恢复；重复已完成矩阵会拒绝重算。假设保证金10%/20%是不同证据层，不能标作真实历史保证金。
- **尚未完成真实接通**：Databento原始 definitions/statistics/status 到标准化清单的自动整合、逐日实际合约/交割期限/历史日历的完整核验、动态历史保证金/券商附加保证金表，以及真实数据全矩阵、统计门槛和共同日期块的不确定性计算。不能只填一个 `verified=true` 就声称外部证据已经核实。

注册表目前是13个根的官方规格模板，具体 instrument_id/到期合约仍为 UNKNOWN。缺某个证券/合约会隔离并留下原因，未运行账户的绩效为 NA；不存在自动合成行情回退。

## 测试与恢复

`python -B -m pytest tests/test_futures_f0_*.py -q -p no:cacheprovider` 运行本模块工程测试（PowerShell中建议由文件列表展开）。测试明确使用人工数据，所有临时输入只在测试临时目录。工程通过不是期货收益或行情权限验收。

数据接通后，开发人员使用 `python -B -m src.futures_f0 run-normalized --input-manifest <已核验清单> --margin 10`，或独立的20%保证金压力层。命令不开放策略参数搜索，不创建前向服务。实际四小时授权过期后需新的有界执行授权；不会自行改截止时间。

引擎每个时段保存有哈希的状态，恢复前核对整套输入哈希。文件锁阻止同时启动多个 F0 进程；每批检查本进程峰值 RSS≤1.5GiB、系统可用内存≥2GiB和本轮时限。越界保留恢复信息后停止自己的研究，不关闭别的服务。

本轮没有启动新的期货前向账户或常驻后台。旧 B1.2 停在原检查点，后续恢复仍须单独处理内存边界。

详见 [合约与来源核验](sources.md)、[登记表](contract_registry.csv) 和 [SPY/QQQ基准口径](BENCHMARK_METHOD.md)。
