# 本轮资源预算

任务开始锚点为 2026-09-14 07:35 UTC，8 小时截止为同日 15:35 UTC。协议在报价时窗结果之前冻结。预算不是允许自动购买服务或模型调用的额度；本轮付费模型调用与新增购买均为零。

只使用已有 Python 虚拟环境、已配置的 Alpaca Basic 历史数据访问与免费公开发行人/SEC 来源。先复用相同证券和查询范围的完整缓存；新增分钟和报价按固定样本、因果候选与实际持仓日期定向取得。

同一时刻只运行一个重负载研究工作进程，原 M20/U 服务独立保留。样本先完成，再执行四本共同资金账户。恢复前必须核对实际进程命令行与最新进度，不能因为界面没有回复就启动副本。

本机可用内存有限。初版全量状态序列化在真实 MRVL 完成后停止，结果只读保留；后续采用 SQLite 归档过期的零消耗报价库存及已处理事件指纹，保留全部原始输入和经济状态。仅在真实成交、权益和恢复严格一致后继续原固定样本。归档为本地恢复材料，不加入验收包。

资源限制、请求失败或数据不足必须保存已完成日期、输入哈希、错误与恢复点，并明确未完成账户；不能把初始化余额当作已完成回测，也不能用 mock 数据填补真实研究输入。
